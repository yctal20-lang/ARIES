services:
  - type: web
    name: aries-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn space_debris_ai.visualization.web_server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
